export default function ProfileLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>
}

export const metadata = {
    title: "Perfil - CustomLab",
    description: "Perfil de usuario",
}